{
  "name": "@proofwork/contracts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "fmt": "forge fmt",
    "snapshot": "forge snapshot",
    "deploy:testnet": "forge script script/Deploy.s.sol:Deploy --rpc-url arc_testnet --account proofwork-deployer --broadcast --verify --verifier blockscout --verifier-url https://testnet.arcscan.app/api -vvv"
  }
}
